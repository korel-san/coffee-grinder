# This file is for LOCAL E2E tests only (do not commit secrets).
#
# Usage:
# 1) Copy to grinder/.env.e2e and fill values
# 2) Run: cd grinder && npm run test:e2e

# Point these to a dedicated TEST spreadsheet (rename it to include "test" or "e2e").
GOOGLE_SHEET_ID_MAIN=
GOOGLE_SHEET_ID_AUTO=

# Cases to seed into the test spreadsheet.
# Recommended: semicolon-separated list of `expect|label|url`:
# E2E_CASES=ok|normal|https://...;fail|missing|https://example.invalid/...
E2E_CASES=

# Backward-compatible single/multi URL formats (all treated as "ok"):
E2E_ARTICLE_URL=
E2E_ARTICLE_URLS=

# Optional: additional fail URLs (comma-separated) if you don't use E2E_CASES.
E2E_FAIL_URLS=

# Auth + API keys (use test keys if possible)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
SERVICE_ACCOUNT_EMAIL=
SERVICE_ACCOUNT_KEY=
OPENAI_API_KEY=
NEWS_API_KEY=

# Keep E2E cheap by overriding models if needed
OPENAI_SUMMARIZE_MODEL=gpt-4o-mini
OPENAI_FACTS_MODEL=gpt-4o-mini-search-preview
OPENAI_VIDEOS_MODEL=gpt-4o-mini-search-preview
OPENAI_WEBSEARCH_CONTEXT_SIZE=low
